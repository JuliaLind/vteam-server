checks:
  javascript: true
build:
  environment:
    node: '20.5.1'
  tests:
    override:
      - 
        command: 'cd server && npm install && npm run test'
        coverage:
          file: 'server/coverage/clover.xml'
          format: 'clover'
