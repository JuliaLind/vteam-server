checks:
  javascript: true
build:
  nodes:
    analysis:
      tests:
        override:
          - 
            command: |
              nvm install 16
              nvm use --delete-prefix v16
              cd server
              npm install
              npm run test
            coverage:
              file: 'server/coverage/clover.xml'
              format: 'clover'
